global angular/cli
npm install -g @angular/cli

init project
ng new <project/name>

create component
ng g c <component/name>

create service
ng g s

labary project
bootstrap, @fortawesome/fortawesome-free